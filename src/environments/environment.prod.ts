
import { VERSION } from './version';

export const environment = {
    production: true,
    i18nPrefix: '',
    test: false,
    envName: 'PROD',
    versions: {
        app: VERSION.version,
    }

};
