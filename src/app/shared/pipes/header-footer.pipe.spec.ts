import { HeaderFooterPipe } from './header-footer.pipe';

describe('HeaderFooterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderFooterPipe();
    expect(pipe).toBeTruthy();
  });
});
